# DESIGN.md

## 产品气质与意象

**编辑部工作台**：一份在清晨排版桌上摊开的选题稿——米白纸面、橙色标记笔、小号等宽数字、密集但呼吸清晰的栏目。用户是同时运营多个公众号的单人创作者，把 100 篇候选文章压缩成 3-5 篇可用对标。界面像专业采编系统，不像"AI 创意 SaaS"。

具象意象：
- 米白纸感背景，橙色（标记笔/印章）作为唯一强调色
- 表格是主角，不是卡片堆叠；行高紧凑、信息密度高
- 数字使用等宽数字，便于纵向扫读
- 标签用小色块 + 图标 + 文字三重编码，色盲可用
- 所有数据都标注来源与时间（"供应商返回的摘要或全文"、"抓取于 12 分钟前"）

## 视觉策略

- **不做**：紫色/蓝紫渐变、玻璃拟态、大幅 hero 插画、通用 AI 后台视觉
- **要做**：编辑部工具感——黑白灰为主、橙色为唯一行动色、标签与状态色克制、细分割线而非阴影
- 图像只出现在：公众号头像、图文卡片预览；不使用装饰性插画

## 配色方案

| Token | Hex | 意象 |
|---|---|---|
| `--canvas` | `#F7F5F2` | 米白纸面，主背景 |
| `--surface` | `#FFFFFF` | 卡面、表格行底、抽屉 |
| `--surface-sunken` | `#F0EDE7` | 折叠区、嵌套面板 |
| `--ink` | `#171717` | 主文字 |
| `--ink-2` | `#525252` | 次级文字 |
| `--ink-3` | `#8A8580` | 辅助文字、时间戳 |
| `--line` | `#E5E1DA` | 边框、分割线 |
| `--line-strong` | `#D6D1C8` | 表头底边、hover 边框 |
| `--brand` | `#F0642B` | 主行动色：搜索按钮、主 CTA、选中态、橙色标记 |
| `--brand-ink` | `#C24A18` | 橙色上的文字/深橙 |
| `--success` | `#2F7A3D` | 低粉爆款标签（墨绿，非亮绿） |
| `--success-bg` | `#E8F1E9` | |
| `--warning` | `#9A6A00` | 数据缺失、查询中 |
| `--warning-bg` | `#FBF1D8` | |
| `--danger` | `#B42318` | 失败、错误 |
| `--danger-bg` | `#FCE8E5` | |
| `--info` | `#1E5FA0` | 信息、缓存命中 |
| `--info-bg` | `#E7EFF8` | |

## 字体排版

- 中文：`PingFang SC` / `Hiragino Sans GB` / `Microsoft YaHei`
- 数字与英文：`ui-sans-serif, system-ui`，配合 `font-variant-numeric: tabular-nums`
- 等宽：`ui-monospace, SFMono-Regular`，用于 requestId、ghid、wx_id、hash
- 字号阶梯：12 / 13 / 14 / 16 / 18 / 22 / 28 px
- 行高：正文 1.5，表格 1.4，标题 1.25
- 字重：常规 400，中号标题/表头 500，数字 500，不滥用 600+

## 布局与间距

- 桌面基准 1440px，内容最大宽 1280px
- 左侧导航 `w-[220px]`，高 100vh，sticky
- 顶部工具栏 `h-16`（64px），sticky top-0，z-30
- 搜索框 `h-12`（48px）
- 表格行最小 `h-[88px]`，头像 64×64
- 间距基数 4px：4 / 8 / 12 / 16 / 20 / 24 / 32
- 移动端 < 768px：导航折叠为顶部栏，表格转卡片，右侧抽屉转全屏 Sheet

## 组件规范

- 圆角：sm 4px / md 6px / lg 10px；不使用大圆角（>12px）
- 阴影：仅弹层/抽屉使用 `0 8px 24px rgba(0,0,0,0.08)`；卡片/表格行不使用阴影
- 按钮主色：橙底白字；次按钮：白底灰边；危险按钮：红字红边浅红底
- 标签（Badge）：高 22px，px-2，圆角 4px，12px 字号，前置 10-12px 图标
- 表格：表头 12px 大写灰字 + 底边；行 hover 用 `--surface-sunken`；选中行用左侧 3px 橙色边 + 浅橙底
- 输入框：48px 高，1px `--line` 边框，focus 时 2px 橙色边框（不留外发光）
- 抽屉：右侧 480px（桌面）/ 全屏（移动），从右滑入
- 进度条：2px 高，橙色填充，灰色轨道

## 动效与交互

- 时长 150-200ms，缓动 `cubic-bezier(0.4, 0, 0.2, 1)`
- 行展开：高度自适应 + opacity 200ms
- 抽屉：transform 250ms
- 标签变化：背景色 200ms cross-fade
- 数字变化：不使用滚动动画（编辑台工具感）
- 所有可点击元素有 hover/focus/active 三态
- 焦点环：2px 橙色 offset 2px

## 状态与可访问性

- 状态同时使用：颜色 + 图标 + 文字标签
  - 低粉爆款：绿底 + TrendingUp 图标 + "低粉爆款"
  - 数据缺失：黄底 + AlertTriangle + "数据缺失"
  - 查询中：黄底 + Loader spin + "账号数据查询中"
  - 失败：红底 + XCircle + 错误短语
- 所有交互元素可键盘到达，focus-visible 可见
- 颜色对比度符合 WCAG AA（正文 ≥ 4.5:1）

## 文案口吻

- 直接、专业、不寒暄
- 数字直给："已获取 1/59 页 · 原始 20/1165 条 · 实际 20 积分"
- 风险提示明确："估算，以各页实际 consumedPoints 为准"
- 数据来源标注完整："供应商返回的摘要或全文"、"预估活跃粉丝（非微信官方精确粉丝数）"
- 错误说明包含下一步动作，不使用"出错了"这类无信息文案

## 实现备注

- 本轮为**高保真前端原型**，所有数据来自 Mock 夹具，页面顶部需要有明确的"MOCK 数据"角标（琥珀色小条）
- Mock 标记使用 `MockBadge` 组件，在生产构建中由环境变量控制隐藏
- 所有外链按钮（打开原文/短链）只允许 `https://mp.weixin.qq.com/`，其他链接在原型中点击只复制 URL 并 toast 提示
